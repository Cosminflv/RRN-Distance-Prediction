version: "3.8"

services:
  flask_server:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      - FLASK_ENV=development
    # Restart automatically if the container crashes (optional)
    restart: on-failure
